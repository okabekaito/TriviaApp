import { ReactNode, VFC } from "react";
import React from "react";

import { Header } from "../organisms/layout/Header";

// type Props = {
//     children : ReactNode;
// };

export const HeaderLayout : VFC = (props) => {
    return (
        <>
            <Header />
            
        </>
    );

}
